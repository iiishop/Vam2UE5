# 阶段 01 验证记录

日期：2026-09-21。环境：本机 UE 5.8.2、引擎自带 Python 3.11.8 / SQLite 3.43.1。来源：`F:\Virt A Mate`。

## 已执行

- 真实资源库首轮索引：5,824 个来源，131,951 个资源；95.3 秒。
- 分类修正后全量重扫：42.4 秒。此时 Windows 文件缓存已预热，不能当作冷启动成绩。
- 最终连续两次增量扫描：约 2.5 秒；第二次变更来源为 0，界面 revision 未变化。
- 查询抽样：用户外观页约 8.7 ms；Morph 搜索约 89.8 ms；外观文件夹页约 6.2 ms。单次本机测量，不是 P95 性能承诺。
- 独立扫描服务完成增量后的工作集抽样约 38 MiB；不包含 UE 和内嵌浏览器进程。
- 当前诊断 66 条，包含来源无效 JSON、损坏或无法读取的包等。其余来源完成索引。
- 14 项自动化测试通过：同名区分、持久化重启、修改 / 删除、取消当前来源回滚、目录枚举失败保留索引、损坏包隔离、旧版分类、读取上限、几何禁读、分页 / 中文搜索、缩略图懒读取 / 命中缓存 / 来源文件哈希不变、缓存上限、损坏后保留并标记旧记录。另有真实 HTTP 服务测试，验证自动检查能够发现新增 / 删除文件，以及拒绝错误会话与跨来源写请求。
- 本地浏览器实际打开同一套 Web 面板，验证默认用户预设页、独立类型标签、48 条分页、详情、翻页与懒加载。一页 48 个卡片时可见图像加载 28 个，其余 20 个维持占位；未发现浏览器错误日志。
- 最终用户外观默认页为 178 条；头发搜索 `ponytail` 返回 363 条、8 页；名称与时间排序交互通过。SQLite 文件约 137.75 MiB。来源包含 5,338 个 VAR 文件和 486 个松散资源目录。

## 最终分类计数

| 类型 | 数量 |
| --- | ---: |
| 外观预设 | 2,226 |
| 发型预设 | 3,135 |
| 服装预设 | 4,027 |
| 皮肤预设 | 871 |
| Morph 预设 | 242 |
| 姿势预设 | 12,358 |
| 人物预设 | 22 |
| 动画预设 | 64 |
| 胸部 / 臀部物理预设 | 61 / 41 |
| 插件预设 | 1,113 |
| 其他预设 | 3,077 |
| 单件发型预设 | 8,865 |
| 单件服装预设 | 15,790 |
| 头发 | 7,249 |
| 服装 | 9,638 |
| 皮肤纹理 | 14,634 |
| Morph | 45,171 |
| 场景文件 | 3,367 |

条目数不等于可独立使用的人物数量，包含多版本包中各自的资源。

## 尚未完成：UE 内编译和加载验收

已实际执行 UE `RunUAT BuildPlugin`。构建在 SDK 校验阶段失败，尚未进入 C++ 编译：

```text
Platform Win64 is not a valid platform to build. SDK validation failed:
Sdk: not found. Required version 10.0.19041.0.
```

本机 `vswhere -all` 也没有返回 Visual Studio 安装。需要安装 UE 支持的 MSVC C++ Build Tools 和 Windows SDK 后，再运行 `Build.ps1`，修复可能的编译错误，检查窗口菜单、原生目录选择、CEF 内嵌面板、关闭 / 重开和退出进程清理。

当前仅有插件源码，没有声称已生成可用 DLL。插件默认不自动启用，避免缺失 DLL 阻碍现有工程启动；编译成功后再启用。浏览器测试不能替代这一步。

## 阶段 02 增补：预设与依赖解析

同日新增 `vam_plan.py`、异步计划 API、计划详情弹窗和历史记录。阶段 01 的 14 项回归测试与阶段 02 的 21 项测试共 **35 项通过**；最终对阶段 02 的严格 JSON / 未解释字段修正后，21 项再次通过。

阶段 02 自动验收覆盖：

- 跨包、SELF、相对纹理、VAM 的 VAJ / VAB 同名伴随资源。
- 精确版本缺失、latest / minN 数字版本排序、动态引用锁定到具体版本。
- 循环与重复引用去重、相同显示名称但不同内容保持独立、同内容二进制复用。
- 声明但未用的缺失依赖不阻断，实际使用却缺失的声明有可定位的缺失项。
- 输入选择顺序不同、重复选择和多次生成，规范化计划字节一致。
- 新增更高版本后锁定重放 ID 不变；原版本实际内容发生改变时阻断。
- 基于独立入库清单区分创建 / 复用 / 更新，保存计划不产生入库状态。
- 原始配置字节和未知字段保留；损坏 JSON、重复键、NaN / 溢出数值、结构类型错误不产生成功状态。
- 取消、源文件在读取期间变更、路径越界、未知引用字段和来源脚本等失败路径。

真实库抽样（本机、文件缓存已预热，非性能承诺）：

| 样本 | 结果 | 用时 |
| --- | --- | ---: |
| 一件服装描述及同名 VAJ / VAB / 相对纹理 | 创建 4，缺失 / 不支持 0，计划就绪 | 约 0.034 秒 |
| 用户外观 `!Mina` | 创建 54，不支持 10；锁定 9 个包引用 | 约 1.15 秒 |
| `A1X.RUDI.1` 外观 | 创建 136，复用 5，不支持 11 | 约 0.83 秒 |

人物样本中的内置人物 / Morph 符号被明确列为 `builtin_adapter_required`；没有伪造人物创建成功。`!Mina` 引用的 `MacGruber.Life.13` 在两个目录中重复，解析器列出两个 VAR 路径并报告歧义，不任意覆盖。

同一套本地 Web 面板已实际验证：选中 VAP → 生成计划 → 不支持项筛选 → 展开“来源文件 / 字段 / 目标” → 打开历史就绪计划 → 验证锁定计划。浏览器无 JavaScript 错误。JSON 计划与配置原文写入本插件 `Saved/Plans`；没有构建人物或写入 VaM 目录。

C++ 编译与 UE 内实际加载仍待用户准备工具链；本阶段未安装系统级组件。

## Builtin 映射增量验收（2026-09-21）

- 生成 2,453 条类型与性别明确的来源映射，关联 199 个 VaM 来源文件；没有复制几何或纹理数据。
- 共 39 项自动测试通过，包括内置同名不同性别、反性别 Morph、未知类型、损坏目录、来源缺失 / 改变、基线复用和锁定复现。
- Rudi 实际预设：147 创建、5 复用、0 缺失、0 不支持；先前 11 个内置阻塞已全部解除。约 1.56 秒生成；落盘后按锁重放得到完全相同计划 ID：2793b507ee21d28ffde7338d8c1407ce829406a956f41036c1d40f6c86d6b403。
- !Mina 实际预设：61 创建、0 缺失、3 不支持；内置映射已全部解决，剩余 3 项均为两个路径重复声明 MacGruber.Life.13 的既有歧义。
- 上文“内置统一不支持”的描述为旧版测试记录，以本节及 STAGE02.md 最新行为为准。
- 用户要求优先完成 builtin，C++ 构建验收暂停；没有声称本次已编译或加载 UE DLL。
