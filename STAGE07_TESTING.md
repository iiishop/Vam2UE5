# Stage07 当前检查点测试说明

这份说明针对已经保存的两个 Cooked 包，不是整个 Stage07 验收，也不是最新工作区源码的重新构建。无需关闭当前编辑器，不安装或覆盖其 DLL。原编辑器中已加载的插件仍是原版本；直接在原窗口点击旧 Stage06 工具不能验证本轮新代码。

## 直接运行

在 PowerShell 中执行，每次只运行一项，等待其自动退出：

```powershell
Set-Location 'I:\Document\UE5\SmartNPC\Plugins\VamResourceBrowser'
./ReplayStage07Evidence.ps1 -Case Baseline -FrameRate 60 -ShowWindow
./ReplayStage07Evidence.ps1 -Case Flesh -FrameRate 60 -ShowWindow
```

需要时可将 FrameRate 改为 30 或 120。这里是帧率上限，不是实际帧率或性能通过声明。脚本核对记录的 EXE 哈希，使用新建的输出目录，运行真实图形游戏并检查退出码、报告和 Flesh 绑定错误；不会重建、修改共享资产或覆盖历史结果。EXE 校验不等于整个包的依赖闭包校验。

启动后等待自动演示完成，不拖动人物或操作暂停，以免干扰自动步骤。两项都没有完整的手动验收流程 UI。报告写入启动时打印的 `Saved/Stage07Manual/<Case>-<ID>/`，即使失败也保留 `game.log`。启动耗时不计入实验时间。

## 应看到什么

| 测试 | 可见效果与范围 | 正常结束的判定 |
|---|---|---|
| Baseline | 三个人物实例、两种独立比例，其中两实例共享资产。基础测试动画及主动呼吸/眨眼、IK/摆姿、局部刚体抓取响应；自动完成形状 Preview/Commit/Cancel、再次抓取、足接触、暂停恢复、移动急停、旋转和停稳。部分变化细微，不是完整行走/玩法演示。 | `Saved/Stage07Composition.json` 的 `composition_passed` 和 `timing_motion_settle_passed` 为 true，三实例各有 3 次 shape_transactions。`stage07_passed:false` 正常。 |
| Flesh | 一个静态人物，角色左胸区域（正面画面的右侧）发生局部受压变化并恢复。压球隐藏以免遮住皮肤，但仍参与接触。未启用重力、主动动画与形状切换。 | 约 20 秒模拟后自动退出，`Saved/FleshCapability.json` 的 reason 为 `capture_complete_pending_visual_and_contact_review`，measured_frames 大于 0，并生成三张截图。`backend_qualified:false` 与 `stage07_passed:false` 均是正确状态。 |

Flesh 时间线：0–4 秒静置，4–8 秒压入，8–12 秒保持，12–16 秒撤出，16–20 秒恢复。截图 `FleshCapability-0/1/2.png` 分别在约 3/10/19 秒取得。应比较三张图中的皮肤变化，不是寻找黄色 witness 或可见球体。

已有基线按预设阈值验证最终 IK/足误差不超过 3 cm、停稳速度不超过 2 cm/s、漂移不超过 0.5 cm。是否满足由报告判断，肉眼不能替代数值检查。

Flesh 已记录关联皮肤探针峰值约 1.724 cm、最大代理体积误差约 3.09%。这是实验观测，不是可接受接触质量的阈值，也不要求每次重放精确相等。局部盒状工程笼还不是合格的人体软组织代理；不要期待坐压、腿臀响应、全身摇动或近景精细压痕。

## 未完成项与责任

| 缺口 | 真实卡点 | 谁来推进 |
|---|---|---|
| Morph/动画/软组织合成 | 实际引擎示例图读取静态顶点，没有 MorphTarget 输入。必须共同处理形状 rest 与表情，避免丢失 Morph 或重复施加形变。 | 由开发实现并做运行回归；不需要用户先测。 |
| 正式局部体积和支承 | 目前只有静态固定支承的盒状笼；动态骨骼附着、弱附着、重力 rest 平衡、可靠接触/释放指标未闭环。 | 由开发实现。用户可后续判断外观是否符合预期。 |
| 可变体型/碰撞输出 | Flesh 尚未接入 ShapeRevision 事务，也未提供版本化软表面/碰撞消费者输出。 | 由开发实现并验证两实例、体型边界和 Cook。 |
| 全身分区及工具 | 大腿/臀部纵切、其它解剖区、权重可视化和完整手动测试入口尚未交付。 | 由开发实现；用户可确认区域边界与优先级。 |
| 其它骨架家族 | 当前两个人物属于同一骨架家族，不能宣称跨家族通过。 | 若有其它家族样本，用户提供明确来源/人物资产；导入、适配和测试仍由开发完成。 |

没有哪项编译、Cook、截图或数值测试目前必须交给用户代做才能继续。现有环境已能运行这些验证。用户最有价值的反馈是：局部变化是否清楚可见、压入/恢复的外观是否自然，以及后续期望的组织区域和样本。看见黑皮肤、灰默认材质、异常拉伸或退出报错时，提供本次输出目录即可，不必重新描述全部步骤。

Windows SDK 所在 E: 盘曾发生读取故障；所需资源编译已通过 I: 隔离缓存继续。该盘是否需要系统维护应另行处理，不是需要用户承担的软组织功能测试，也不是目前停止开发的理由。

当前源码的重新编译和联合回归仍是开发责任。不要直接把旧 CompositionRun 传入 `RunStage07Cooked.ps1` 来代表新增 Flesh 源码通过；源码身份检查会正确拒绝过期宿主。
