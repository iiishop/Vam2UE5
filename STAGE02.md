# 阶段 02：预设与依赖解析

本阶段产出**确定的来源资源导入计划**，不是人物资产。`ready` 表示当前支持范围内的引用和来源检查通过，不表示人物已创建，或已通过 UE 外观还原验收。

## 界面入口

1. 选择资源，点击右侧 **生成导入计划**。
2. 在弹窗按 **创建、复用、更新、缺失、不支持** 过滤条目。
3. 展开条目的 **引用来源**，查看原始来源文件、JSON Pointer 字段位置和原始目标。
4. 查看 **最终版本锁定** 和 **声明依赖与实际使用**。
5. 点击 **验证锁定计划**，使用原来选定的具体包版本重放，并验证配置 / 资源内容指纹。
6. 顶栏 **导入计划** 可重新打开最近 50 个已保存计划。每个计划的完整保存路径显示在弹窗中；条目列表每页 100 条。

计划生成在独立工作线程执行，并发为 1，可取消。原有目录扫描和资源浏览仍可用。

## 引用规则

| 输入 | 当前规则 |
| --- | --- |
| `Creator.Package.3:/Custom/...` | 只选择第 3 版。缺失时报告缺失版本，不改用其他版本 |
| `Creator.Package.latest:/...` | 在已索引、已安装的数字版本中取最大版本 |
| `Creator.Package.min3:/...` | 在已安装且版本 ≥ 3 的包中取最大版本 |
| `SELF:/...` | 当前 VAR；松散预设没有 SELF 包上下文，报告缺失 |
| `Custom/...`、`Saves/...`、`Assets/...` | 包内根路径优先；不存在时检查 VaM 松散根目录 |
| `fabric.png`、`../Textures/a.png` | 相对引用它的配置文件目录；不允许越过来源根目录 |
| 同一包 ID 对应多份 VAR | 报告歧义并列出全部位置，不任意选择一份 |
| ZIP 中重复的同路径条目 | 报告歧义，不依赖 ZIP 实现的覆盖顺序 |
| URL、绝对操作系统路径 | 不支持，不联网下载、不读取 VaM 根目录外文件 |

`latest` 和 `minN` 均在本机真实 `meta.json` 中观察到，实际 VAP 中也观察到 `latest`、指定版本、SELF；上表是本导入器明确固定的选型策略，不声称复刻 VaM 所有包管理偏好。不会读取外部来源脚本来决定引用。

VAP、旧版人物 JSON、VAM、VMI、VAJ 都采用严格 JSON 解析，拒绝重复键、非法数值、损坏内容和已识别结构的类型错误。所有参数值保持原类型，包括以字符串保存的浮点数和布尔值。

VAM 服装 / 发型条目会加入同名 VAJ 和 VAB；含顶点差异的 VMI 会加入同名 VMB。VAJ 内的相对纹理继续递归解析。VAB / VMB / 图片只做流式 SHA-256 校验，不解码网格或图片。

## 声明和实际使用

- `meta.json.dependencies` 原样保留，包含嵌套声明；不会因为包声明了某依赖就导入整个包。
- 实际依赖由选中配置及其可达配置中的引用生成。
- 未实际引用的缺失声明不阻断计划。真正引用却缺失的资源一定生成缺失项。
- 显式 `enabled=false` 的条目和权重为 0 的 Morph 引用不加入活动依赖，记录到 `inactive_references`，原参数仍保留。
- 循环记录在 `cycles` 和依赖边中；递归展开在已访问节点停止，不重复生成资源。当前保存图结构，不尝试构建循环对象或确定 UE 创建顺序。
- 未知字段全部保留；未知字段中的明显资源引用会标记为“不支持，需检查”，不会默默忽略后宣称完成。

这是静态引用分析，不执行来源程序，因此插件脚本动态拼接的引用、运行时条件和插件专有格式不在支持范围内。

## 五类导入项

| 类型 | 判定 |
| --- | --- |
| 创建 | 当前已入库清单没有此来源资源 |
| 复用 | 已入库清单中同来源的 SHA-256 相同；或计划内同类型二进制内容已出现 |
| 更新 | 已入库清单中同来源存在，但 SHA-256 不同 |
| 缺失 | 缺失实际引用的版本、文件或上下文 |
| 不支持 | 损坏配置、包歧义、未解释引用、超出预算、外部路径、脚本 / 未支持格式、需内置映射等 |

来源身份由“来源 VAR 相对路径 + 包内路径”（松散文件为根目录相对路径）生成。同名不同路径共存，不按显示名称覆盖。相同二进制内容可复用；配置文件即使字节相同也不跨来源去重，因为相对引用和 SELF 的上下文可能不同。

已入库清单接口为插件 `Saved/ImportState/manifest.json`：

```json
{
  "schema": 1,
  "assets": {
    "资源稳定ID": {
      "sha256": "64位SHA256",
      "destination": "/Game/Characters/Example"
    }
  }
}
```

目前没有人物构建器，也没有已入库资产；默认清单为空。**生成或保存计划绝不写入这份清单**，不会把“上次规划过”冒充“已导入”。后续实际导入器完成资产提交后才应维护它。创建 / 复用 / 更新是来源资源层的动作，不是已经完成的 UE 派生资产操作。

## 内置资源

`Female Custom`、人物名称、内置服装 / 发型名称和不带文件路径的 Morph ID 作为符号引用单独保留，带 `source=builtin` 与资源类别。

`Config/BuiltinCatalog.json` 现在提供来自本机 VaM 的可验证来源映射：45 个人物、101 件服装、15 个头发条目（含 No Hair）、2,292 个 Morph。人物、服装和头发读取实际 `a_per` Person 注册表，解析 AssetBundle 容器位置及 manifest 依赖；Morph 来自 `f_mb` / `m_mb`，记录对象 ID、数组位置、内部名称和参数元数据，跳过 delta 数组，不解码网格。

计划生成时按类型、精确名称和人物性别解析；`morphsOtherGender` 使用另一性别。没有性别上下文且命中多个不同对象时报告 `builtin_ambiguous`，不猜测。No Hair 映射为 `clear_hair` 操作。未知标识继续报告 `builtin_adapter_required`；映射来源文件缺失报告 `builtin_file_missing`；来源 SHA-256 或大小与目录不符报告 `builtin_catalog_stale`。

已确认条目包含 `builtin_mapping`（原始登记证据、对象定位、所有来源文件及 SHA-256），参与创建 / 复用 / 更新判断与锁定复现。GUI 可展开“内置映射”查看。这里的“创建”指计划中的待转换项，**并未生成 UE 人物、网格、材质或 Morph 资产**。

目录只覆盖已验证的 VaM 文件版本，不代表任意版本或第三方注册标识都受支持。更新 VaM 后需重新生成目录；离线工具需要独立 Python 3.11+ 和 `UnityPy==1.25.3`，编辑器运行时仍只用标准库：

```powershell
python Plugins/VamResourceBrowser/Scripts/build_builtin_catalog.py 'F:\Virt A Mate' Plugins/VamResourceBrowser/Config/BuiltinCatalog.json
```

生成器只写输出 JSON；原始 VaM 目录保持只读。目录不含网格、纹理或 Morph delta 内容。保存计划后修改映射或源文件会阻止原计划被报告为成功复现。

## 可复现与原始数据

- `Saved/Plans/<plan_id>.json`：完整计划，包含五类条目、依赖图、声明、版本锁、原始参数和未解释字段。
- `Saved/Plans/Objects/<sha256>`：配置原始字节，包括原始空白和编码字节。损坏配置在读取成功后也保留原始字节。
- `plan_id` 是除自身之外整个规范化计划的 SHA-256；不混入生成时间、扫描顺序、随机标识或耗时。同一输入、文件内容、包集合及入库基线，多次生成得到相同字节和 ID。
- 重放锁定计划时不升级动态引用；安装新版本不会改变原来锁定的选择。检查实际资源、包元数据及入库基线指纹，变化时阻断，不声称已复现。
- 同版本 VAR 被重新打包且使用中的内容改变，也会被指纹检测。没有保留整份二进制资源副本，因此原始二进制被删除后，锁定计划会报告缺失，而不能凭计划恢复它。
- 当前未使用的声明可用性作为原计划的观察快照保留；重放不会因为新安装了一个未使用包而更改原计划。

## 上限与故障

单次最多 32 个选择、4,096 个图节点、20,000 条引用 / 声明、256 个包，引用递归深度最多 128 层。同时最多保留 4 个 ZIP 中央目录；其大小由阶段 01 的 ZIP 上限限制。

单配置 8 MiB，配置原文合计 64 MiB，单二进制 256 MiB，单计划总读取 2 GiB。计划存储合计 1 GiB，超限时报错而不是删除用户的旧计划。读写失败、取消、解析损坏、来源在生成期间变化都会生成受阻 / 失败状态，不能显示就绪。

计划包含原始参数，内容可能较大。界面只返回分页条目，完整参数留在本地 JSON；服务最多缓存最近读取的一个计划。原始 VaM 文件保持只读。

## 验收与边界

执行 `Tests/test_plan.py`。测试覆盖跨包、SELF、相对路径、数字版本排序、minN、缺失版本、循环、重复引用、同名异内容、内容复用、入库清单更新、原始字段 / 字节保存、严格损坏配置处理、动态版本锁定重放、同版本改内容检测，以及确定性输出。

本阶段沿用阶段 01 的 UE 编辑器宿主；按用户最新要求暂不处理 C++ 编译，当前验收针对解析器、计划存储与浏览器。具体实测记录见 `VALIDATION.md`。
